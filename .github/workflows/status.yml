name: ✅ Status Check

on:
  push:
    branches:
      - main

jobs:
  status_check:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run status check script
      run: bash cmd/status_check.sh